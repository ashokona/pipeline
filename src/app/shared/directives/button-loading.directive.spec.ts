import { ButtonLoadingDirective } from './button-loading.directive';

describe('ButtonloadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
