import { UnderDevDirective } from './under-dev.directive';

describe('UnderDevDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderDevDirective();
    expect(directive).toBeTruthy();
  });
});
