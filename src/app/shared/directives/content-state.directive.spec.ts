import { ContentStateDirective } from './content-state.directive';

describe('ContentStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentStateDirective();
    expect(directive).toBeTruthy();
  });
});
