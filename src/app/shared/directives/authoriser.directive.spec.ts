import { AuthoriserDirective } from './authoriser.directive';

describe('AuthoriserDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthoriserDirective();
    expect(directive).toBeTruthy();
  });
});
